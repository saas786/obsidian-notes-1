# 5. Image Enhancement and Spatial Filtering
---

## Histogram
A function that counts the number of observations that fall into a series of disjoint sets called **bins**.

$K$: number of bins
$B_{k}$: $k$th bin, 
